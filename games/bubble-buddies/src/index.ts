/**
 * Bubble Buddies — game module entry point.
 *
 * The deterministic sim (src/sim/) is implemented from SPEC.md in Phase 1
 * step 4. This stub exists so the workspace wiring is verifiable end to end.
 */
import { TICKS_PER_SECOND } from '@retro-recall/retrokit';

export const GAME_ID = 'bubble-buddies';
export const SIM_TICK_RATE = TICKS_PER_SECOND;
